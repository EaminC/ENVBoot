# EnvBoot Configuration
# Copy this file to config/.env and customize for your environment.
# These values are used by test scripts if not overridden via environment variables.

# SSH keypair name (must exist in your OpenStack project)
KEY_NAME=Chris

# OS image name or ID
IMAGE=CC-Ubuntu20.04

# Instance flavor
FLAVOR=baremetal

# Network name or ID
NETWORK=sharednet1

# Security group (must allow TCP/22 for SSH)
SECURITY_GROUP=default

# Optional: OpenStack region
# REGION=CHI@UC
